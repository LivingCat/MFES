class Supplier
instance variables
  name:seq of (char);
	inv len name > 0;

operations
	public Supplier: seq of (char) ==> Supplier
		Supplier(n) == (name := n; return self);
		
	public getName: () ==> seq of (char)
		getName() == return name;
		
end Supplier
