class Client
instance variables
  name: seq of (char);
  inv len name > 0;

operations
	public Client: seq of (char) ==> Client
		Client(n) == (name := n; return self);
		
	public getName: () ==> seq of (char)
		getName() == return name;

end Client